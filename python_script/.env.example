# .env.example — copy to .env and fill in your values
# Currently no API keys are required (all public endpoints).
# Add keys here when integrating paid data sources (e.g. Coinalyze).

# COINALYZE_API_KEY=your_key_here
# COINMARKETCAP_API_KEY=your_key_here  # needed for Model 4
